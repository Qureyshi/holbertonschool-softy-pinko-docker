# Use the latest Ubuntu image
FROM ubuntu:latest

# Update and upgrade the packages
RUN apt-get update && apt-get upgrade -y

# Set the command to echo "Hello, World!"
CMD ["bash", "-c", "echo Hello, World!"]